# ============================================================================
# Shared Layer Interfaces
# ============================================================================
# Header-only layer interface definitions shared between CPU and CUDA
# implementations. No source files needed for now.
# ============================================================================

# This is a header-only component — no library target needed.
# Layer headers live in include/ and are accessed via the global
# include_directories() set in the root CMakeLists.txt.
