# ============================================================================
# src/ — All source code subdirectories
# ============================================================================

add_subdirectory(cpu)
add_subdirectory(cuda)
add_subdirectory(layers)
add_subdirectory(benchmarks)
